export class MenuGroup {
    id: number;
    name: string;
    logoPath: string;

    constructor(id: number, name: string, logoPath: string) {
        this.id = id;
        this.name = name;
        this.logoPath = logoPath;
    }
}