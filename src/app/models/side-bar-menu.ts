export class SideBarMenu {
    logoPath: string;
    groupType: string;

    constructor(logoPath: string, groupType: string) {
        this.logoPath = logoPath;
        this.groupType = groupType;
    }

}